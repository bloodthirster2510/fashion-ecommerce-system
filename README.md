# fashion-ecommerce-system

## Try-on development

From the backend folder, run the local try-on dev stack:

```powershell
cd backend
npm run dev:try-on
```

This starts the backend plus the local image validation and garment processing
services. ComfyUI still needs to be opened separately when testing the real
Comfy provider.
